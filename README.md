# Energy Manager Addon

Home Assistant add-on repository for the Energy Manager Agent.

This repository contains the add-on metadata only. The agent itself is pulled as a pre-built
container image from a private registry, so the add-on is usable by its operator only.

## Installation

Add-on Store -> Repositories -> add the URL of this repository.
